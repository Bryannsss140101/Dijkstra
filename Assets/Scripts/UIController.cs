using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class UIController : MonoBehaviour
{
    [SerializeField] private TMP_Dropdown dropdownOrigin;
    [SerializeField] private TMP_Dropdown dropdownDestination;
    [SerializeField] private GraphVisualizer graphVisualizer;

    private void Start()
    {
    }

    private void Update()
    {
    }
}